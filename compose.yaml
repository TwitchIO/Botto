services:
  twitchio-botto:
    build: .
    container_name: twitchio-botto
    image: twitchio-botto
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./keys:/app/keys:ro
